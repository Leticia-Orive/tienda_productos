// Generated by GitHub Copilot
// Para que sirve: Captura errores de renderizado de React y muestra una pantalla de recuperacion.
import { Component } from 'react';

/**
 * ErrorBoundary catches unhandled render/lifecycle errors in its subtree and
 * shows a user-friendly fallback instead of a blank screen.
 *
 * Security note: The error message shown to the user is intentionally vague to
 * avoid leaking implementation details. Full error info is only logged to the console.
 */
export default class ErrorBoundary extends Component {
  constructor(props) {
    super(props);
    /** @type {{ hasError: boolean, errorId: string | null }} */
    this.state = { hasError: false, errorId: null };
    this.handleRetry = this.handleRetry.bind(this);
  }

  /**
   * Derives error state from a thrown error.
   * @returns {{ hasError: boolean, errorId: string }}
   */
  static getDerivedStateFromError() {
    const errorId = typeof crypto !== 'undefined' && crypto.randomUUID
      ? crypto.randomUUID()
      : String(Date.now());
    return { hasError: true, errorId };
  }

  /**
   * Logs error details for debugging without exposing them in the UI.
   * @param {Error} error
   * @param {import('react').ErrorInfo} info
   */
  componentDidCatch(error, info) {
    // Intentionally log only to console — never send to an external service
    // without user consent and data-protection review.
    console.error('[ErrorBoundary] Render error caught:', error, info.componentStack);
  }

  /** Resets error state so the user can retry. */
  handleRetry() {
    this.setState({ hasError: false, errorId: null });
  }

  render() {
    if (this.state.hasError) {
      return (
        <main
          className="min-h-screen flex items-center justify-center bg-gray-50 px-4"
          role="alert"
          aria-live="assertive"
        >
          <div className="max-w-md w-full bg-white rounded-2xl shadow-lg p-8 text-center">
            <p className="text-5xl mb-4" aria-hidden="true">⚠️</p>
            <h1 className="text-2xl font-bold text-gray-900 mb-2">
              Algo salió mal
            </h1>
            <p className="text-gray-500 mb-1">
              Ocurrió un error inesperado en la aplicación.
            </p>
            {this.state.errorId && (
              <p className="text-xs text-gray-400 mb-6 font-mono">
                ID: {this.state.errorId}
              </p>
            )}
            <div className="flex flex-col sm:flex-row gap-3 justify-center">
              <button
                type="button"
                onClick={this.handleRetry}
                className="bg-indigo-600 hover:bg-indigo-700 text-white font-medium px-6 py-2 rounded-lg transition focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600"
              >
                Reintentar
              </button>
              <button
                type="button"
                onClick={() => { window.location.href = '/'; }}
                className="border border-gray-300 hover:bg-gray-50 text-gray-700 font-medium px-6 py-2 rounded-lg transition focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gray-400"
              >
                Volver al inicio
              </button>
            </div>
          </div>
        </main>
      );
    }

    return this.props.children;
  }
}
