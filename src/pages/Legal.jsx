// Generated by GitHub Copilot
import { useEffect, useRef, useState } from 'react';
import { Link, useLocation } from 'react-router-dom';
import useDocumentTitle from '../hooks/useDocumentTitle';
import useLanguage from '../context/useLanguage';

/**
 * Legal page with terms, privacy, and returns anchors used by footer links.
 */
export default function Legal() {
  const { t } = useLanguage();
  const { hash } = useLocation();
  const [liveAnnouncement, setLiveAnnouncement] = useState('');
  const pageHeadingRef = useRef(null);

  useEffect(() => {
    if (!hash) {
      setLiveAnnouncement('');
      pageHeadingRef.current?.focus();
      return;
    }

    const sectionId = hash.replace('#', '');
    const section = document.getElementById(sectionId);
    const heading = section?.querySelector('h2');

    if (!section || !heading) {
      setLiveAnnouncement(t('legal.title'));
      pageHeadingRef.current?.focus();
      return;
    }

    section.scrollIntoView({ behavior: 'smooth', block: 'start' });
    heading.focus({ preventScroll: true });
    setLiveAnnouncement(heading.textContent || '');
  }, [hash, t]);

  useDocumentTitle(t('legal.title'));

  return (
    <main className="max-w-4xl mx-auto px-4 py-8">
      <p className="sr-only" role="status" aria-live="polite" aria-atomic="true">
        {liveAnnouncement}
      </p>

      <h1 ref={pageHeadingRef} className="text-3xl font-bold text-gray-900 mb-2" tabIndex={-1}>{t('legal.title')}</h1>
      <p className="text-gray-600 mb-8">{t('legal.subtitle')}</p>

      <nav aria-label={t('common.breadcrumb')} className="mb-6 text-sm text-gray-500">
        <Link to="/" className="hover:text-indigo-600 transition focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600 rounded-sm">
          {t('notFound.home')}
        </Link>
        <span className="mx-2" aria-hidden="true">/</span>
        <span aria-current="page" className="text-gray-700 font-medium">{t('legal.title')}</span>
      </nav>

      <section id="terms" className="mb-8 rounded-xl border border-gray-200 bg-white p-5 scroll-mt-24" aria-labelledby="legal-terms-title">
        <h2 id="legal-terms-title" tabIndex={-1} className="text-xl font-semibold text-gray-900 mb-2 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">{t('legal.termsTitle')}</h2>
        <p className="text-gray-700 leading-relaxed">{t('legal.termsBody')}</p>
      </section>

      <section id="privacy" className="mb-8 rounded-xl border border-gray-200 bg-white p-5 scroll-mt-24" aria-labelledby="legal-privacy-title">
        <h2 id="legal-privacy-title" tabIndex={-1} className="text-xl font-semibold text-gray-900 mb-2 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">{t('legal.privacyTitle')}</h2>
        <p className="text-gray-700 leading-relaxed">{t('legal.privacyBody')}</p>
      </section>

      <section id="returns" className="mb-8 rounded-xl border border-gray-200 bg-white p-5 scroll-mt-24" aria-labelledby="legal-returns-title">
        <h2 id="legal-returns-title" tabIndex={-1} className="text-xl font-semibold text-gray-900 mb-2 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">{t('legal.returnsTitle')}</h2>
        <p className="text-gray-700 leading-relaxed">{t('legal.returnsBody')}</p>
      </section>

      <p className="text-sm text-gray-500">{t('legal.lastUpdated')}</p>
    </main>
  );
}
