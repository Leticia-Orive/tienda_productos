// Generated by GitHub Copilot
// Para que sirve: Hook de consumo del contexto de idioma y localización.
//   Centraliza el acceso a language, locale, setLanguage, t(), formatCurrency(),
//   formatDateTime(), translateCategory() y translateProductText().
//   Lanza un error claro si se usa fuera de LanguageProvider para facilitar el debugging.
// Flujo principal: componentes llaman a useLanguage() → obtienen LanguageContext.value.
// Dónde tocar cambios: sólo si el contrato de LanguageContext.value cambia en LanguageContext.jsx.
import { useContext } from 'react';
import { LanguageContext } from './LanguageStateContext';

/**
 * Custom hook to consume LanguageContext safely.
 * @returns {{ language: string, locale: string, languages: Array<{ code: string, label: string, locale: string }>, setLanguage: Function, t: Function, formatCurrency: Function, formatDateTime: Function, formatDateRange: Function, translateCategory: Function, translateProductText: Function }}
 */
export default function useLanguage() {
  const context = useContext(LanguageContext);
  if (!context) {
    throw new Error('useLanguage debe usarse dentro de un LanguageProvider');
  }

  return context;
}