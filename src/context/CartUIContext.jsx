// Generated by GitHub Copilot
import { createContext } from 'react';

/**
 * CartUIContext: Provides toast/UI feedback for cart operations.
 * Consumed only by Toast component and callbacks that need to notify users.
 * Separation allows ProductCard to avoid re-renders when toasts appear.
 */
export const CartUIContext = createContext(null);
